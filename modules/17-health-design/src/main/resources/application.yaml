server:
  port: 8107
spring:
  application:
    name: health-design-lab
management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info

