server:
  port: 8106
spring:
  application:
    name: connection-pool-lab
  datasource:
    url: jdbc:h2:mem:pool;DB_CLOSE_DELAY=-1
    hikari:
      maximum-pool-size: 2
      connection-timeout: 500

