server:
  port: 8099
spring:
  application:
    name: n-plus-one-lab
  datasource:
    url: jdbc:h2:mem:nplusone;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

