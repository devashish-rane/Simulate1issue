server:
  port: 8103
spring:
  application:
    name: outbox-lab
  datasource:
    url: jdbc:h2:mem:outbox;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

